name: layz
description: LAYZ — Your physique. Your pace. No excuses.
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # UI
  google_fonts: ^6.1.0

  # Storage
  shared_preferences: ^2.2.2

  # Sensors (parallax effect on today card)
  sensors_plus: ^4.0.2

  # (Future) Auth + database
  # supabase_flutter: ^2.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true